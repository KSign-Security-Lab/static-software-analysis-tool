# Test Fixtures

This directory contains test data for comparison tests.

## Structure

- `c_sources/` - C source files for testing
- `cpg/` - Pre-generated CPG JSON files
- `expected/` - Expected output files (optional, for reference)

## Usage

Fixtures are used by comparison tests to ensure Node.js and Python versions produce identical outputs.

