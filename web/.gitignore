/node_modules
/.next
/out
/build
*.tsbuildinfo

# The repo-root .gitignore excludes "lib/" (Python); our TS source lives in lib/.
!/lib
!/lib/**
next-env.d.ts.bak
.env*.local
.DS_Store
npm-debug.log*

# Alternate build dirs (NEXT_DIST_DIR), so a build can run beside a dev server.
.next-*
