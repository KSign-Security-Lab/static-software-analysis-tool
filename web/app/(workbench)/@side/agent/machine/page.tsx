import ExplorerPane from "@/features/agent/ExplorerPane";

/**
 * The files, still: a breakpoint and a replay are about code that was read, and
 * knowing which file you are looking at is part of that.
 */
export default function Slot() {
  return <ExplorerPane />;
}
