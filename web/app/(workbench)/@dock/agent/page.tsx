/**
 * Nothing.
 *
 * It held the findings, interleaved with every tool call the agent made, in a
 * shelf that cost the editor a third of its height. Findings are the navigator's
 * 문제 list now -- they are the answer, not output accumulating while you work
 * on something else -- and the calls are steps in a finding's 판단 과정, which is
 * the only context anyone ever wanted one in.
 */
export default function Slot() {
  return null;
}
