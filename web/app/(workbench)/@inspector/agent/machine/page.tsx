import InspectorPane from "@/features/agent/InspectorPane";

/** The record: every call the run made, in the space that is about the run. */
export default function Slot() {
  return <InspectorPane />;
}
